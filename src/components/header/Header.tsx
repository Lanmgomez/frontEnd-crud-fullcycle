import React from "react";

import "./Header.scss";
const Header = () => {
  return (
    <header className="header">
      <p className="header-text">Bem vindo, Islan!</p>
    </header>
  );
};

export default Header;
